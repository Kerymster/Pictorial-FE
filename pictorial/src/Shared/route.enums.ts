export enum MAIN_ROUTES {
  MAIN = "/*",
  HOME = "/home",
  LOGIN = "/login",
}

export enum APP_ROUTES {
  HOME = "/home",
  ABOUT = "/about",
  STORIES = "/stories",
  GALLERY = "/gallery",
  MARKETPLACE = "/marketplace",
  STORYDETAIL = "/story-detail",
  NOTFOUND = "*",
}
